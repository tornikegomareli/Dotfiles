# Runtime state and junk — config only in this repo.
.DS_Store

# App runtime/telemetry dirs, not config
Battle.net/
cagent/

# herdr runtime output
herdr/default.profraw
herdr/release-notes.json

# vercel plugin session state
vercel-plugin/active-session.json

# Backup files
*.bak
*.bak.*
tilebar/config.toml.bak*
tilebar/config.toml.before-*
tilebar/config.toml.e2e-backup
tilebar/set-test-tile.sh

# Secrets
.env

# herdr plugin checkouts (reinstall from herdr/plugins.json)
herdr/plugins/github/

# App runtime state and caches
raycast/
hunk/state.json
default.profraw
aerospace/aerospace.toml.backup-*
