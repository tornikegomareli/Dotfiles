#!/usr/bin/env node
import { f as a } from "./client-DfrJv231.js";
import { p as u, H as c } from "./color-Bf2jGwiV.js";
const l = ({ args: o }, { threadID: t }) => u(async (s) => {
  try {
    const r = `/api/web-search?query=${encodeURIComponent(o.query)}&numResults=${o.num_results || 5}`, e = await a(r, {
      method: "GET",
      signal: s,
      headers: t ? { [c]: t } : void 0
    });
    if (!e.ok) {
      const n = await e.text();
      throw new Error(`Server returned ${e.status}: ${n}`);
    }
    return {
      status: "done",
      result: await e.json()
    };
  } catch (r) {
    throw new Error(`Failed to perform web search: ${r.message}`);
  }
});
export {
  l as nodeWebSearchTool
};
//# sourceMappingURL=node-DUQOAXRE.js.map
